[user]
	email = ninocangialosi@yahoo.it
	name = Antonino Cangialosi
#[core]
#    pager = delta

#[interactive]
#    diffFilter = delta --color-only

[diff]
    tool = vimdiff
[delta]
    navigate== true    # use n and N to move between diff sections
    light = false      # set to true if you're in a terminal w/ a light background color (e.g. the default macOS terminal)
    side-by-side = true
    line-numbers = true

[merge]
    tool = vimdiff1
#    conflictstyle = diff3
[mergetool]
    prompt = false
[vimdiff]
    colorscheme = codedark
[rerere]
    enabled = true
[http]
    sslVerify = false
    proxyAuthMethod = basic
[https]
    sslVerify = false
    proxyAuthMethod = basic
[core]
    autocrlf = false
    eol = lf
#    pager = delta
